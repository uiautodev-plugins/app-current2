{
  "private": true,
  "scripts": {
    "fetch-types": "curl -s http://localhost:33299/plugin-runtime.d.ts -o plugin-runtime.d.ts",
    "dev": "npm run dev:js & npm run css:watch",
    "dev:js": "esbuild app.tsx --bundle --outfile=app.js --jsx=automatic --jsx-import-source=preact --watch",
    "css": "tailwindcss -i ./styles.css -o ./app.css --minify",
    "css:watch": "tailwindcss -i ./styles.css -o ./app.css --watch",
    "build": "npm run css && esbuild app.tsx --bundle --outfile=app.js --jsx=automatic --jsx-import-source=preact",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "prettier": "^3.8.3",
    "tailwindcss": "^3.4.19",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "lucide-preact": "^1.43.0",
    "preact": "^10.26.5"
  }
}
